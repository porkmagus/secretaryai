"use client";

import type { ReactNode } from "react";

export type DeskSidebarProps = {
  children?: ReactNode;
};

export function DeskSidebar({ children }: DeskSidebarProps) {
  return children;
}
