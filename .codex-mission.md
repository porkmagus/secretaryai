# Codex Mission: SecretaryAI Phase 3-5 Completion

You are operating in `/Users/sean/repos/secretaryai`.
This is a TypeScript npm workspaces monorepo (apps/web Next.js 16, apps/worker Fastify 5, packages/* shared).

## CRITICAL CONSTRAINTS
- `esModuleInterop: false` — use named imports only (`import { test }` not `import test`)
- `allowImportingTsExtensions: false` — import paths omit file extensions
- Node.js >=24, TypeScript 5.9.3
- Tests use `node:test` + `node:assert` + `tsx` loader
- Build gate: `npm run typecheck` must pass, `npm run build` must pass, `npm run test --workspace @secretary/worker` must pass

## PHASE 3 — Expand Test Coverage

### Task 3.1: Test `chat-persistence.ts` helpers
File: `apps/worker/src/lib/chat-persistence.ts`
Read it. Identify any pure functions (no DB calls, no side effects). If none exist, test the shape/validation logic by extracting a `validateMessageInsert()` pure helper and test that.
Create: `apps/worker/src/lib/chat-persistence.test.ts`
Use `import { test } from "node:test"` and `import { strict as assert } from "node:assert"`.
Run: `npm run test --workspace @secretary/worker` — must pass.

### Task 3.2: Test `turn-orchestrator.ts` pure branches
File: `apps/worker/src/lib/turn-orchestrator.ts`
Read it. Find any pure decision/stateless functions (e.g., `resolveTurnIntent`, `classifyMessageType`, `selectReplyMode`). Create `apps/worker/src/lib/turn-orchestrator.test.ts` covering each branch.
If the file is entirely side-effectful, skip with a comment in the test file explaining why.

### Task 3.3: Test `telegram-integration.ts` parsing
File: `apps/worker/src/lib/telegram-integration.ts`
Read it. Find pure parsing functions (e.g., `extractTelegramCommand`, `parseUpdatePayload`, `sanitizeMessageText`).
Create `apps/worker/src/lib/telegram-integration.test.ts` using inline JSON fixtures.
Cover: command extraction (`/start`, `/help`, `/task`), message text parsing, bot mention stripping.

### Task 3.4: Voice regression script dry mode
File: `scripts/phase4/verify.mjs`
Read it. Add a `--dry` CLI flag. When `--dry` is passed, skip any live service checks and instead validate: (a) required env vars exist, (b) expected directory structure under `runtime/speech/` exists, (c) log a "would check TTS model" message.
Add prerequisite comment block at top of file.
Run: `node scripts/phase4/verify.mjs --dry` — must exit 0.

## PHASE 5 — Decompose Oversized Files

### Task 5.1: Decompose `desk-shell.tsx`
File: `apps/web/app/desk-shell.tsx` (~1,172 lines)
Create sub-directory `apps/web/app/desk-shell/`
Extract into:
- `chat-input.tsx` — input area, send button, followup chips, keyboard shortcuts
- `message-list.tsx` — scrollable message rendering, avatar, timestamps, status indicators
- `sidebar.tsx` — navigation rail, conversation list, search/filter
- `desk-shell.tsx` — orchestrator: state management, data fetching, layout shell
Keep all existing types, hooks, and accessibility attributes. Do NOT change behavior.
After extraction, run: `npm run typecheck && npm run build --workspace @secretary/web`

### Task 5.2: Decompose `agent-jobs.ts`
File: `apps/worker/src/lib/agent-jobs.ts` (~2,733 lines)
Create files in same directory:
- `agent-job-lifecycle.ts` — createJob, cancelJob, resumeJob, finalizeJob
- `agent-job-execution.ts` — runStep, executeToolLoop, runVerification
- `agent-job-requirements.ts` — extractRequirements, insertRequirements, resolveRequirements
- `agent-job-artifacts.ts` — storeArtifact, getArtifact, listArtifacts
- `agent-jobs.ts` — barrel re-export of all public functions
Move types/interfaces to `agent-job-types.ts` if they don't already live in `types.ts`.
Update all imports in consumers (`apps/worker/src/lib/*.ts`, `apps/worker/src/routes/*.ts`).
After extraction, run: `npm run typecheck && npm run test --workspace @secretary/worker`

### Task 5.3: Decompose `admin-runtime.ts`
File: `apps/worker/src/lib/admin-runtime.ts` (~1,486 lines)
Create files in same directory:
- `admin-import-export.ts` — importSettings, exportSettings, validateImportJson
- `admin-snapshots.ts` — createSnapshot, restoreSnapshot, listSnapshots
- `admin-runtime.ts` — remaining orchestration (runtime status, health, config)
Update imports in consumers.
After extraction, run: `npm run typecheck && npm run test --workspace @secretary/worker`

## DONE WHEN
- `npm run typecheck` exits 0
- `npm run build` exits 0
- `npm run test --workspace @secretary/worker` exits 0 with 60+ tests
- `node scripts/phase4/verify.mjs --dry` exits 0
- All new files have correct import style (named imports, no `.ts` extensions)

## VERIFICATION COMMANDS (run after every batch)
```bash
cd /Users/sean/repos/secretaryai
npm run typecheck 2>&1 | tail -5
npm run build 2>&1 | tail -5
npm run test --workspace @secretary/worker 2>&1 | tail -10
```

If any command fails, STOP and fix before proceeding to next task.
